define([ 'angular' ], function(angular) {
	'use strict';

	/**
	 * @ngdoc function
	 * @name icosvrWebApp.controller:TermsCtrl
	 * @description # TermsCtrl Controller of the icosvrWebApp
	 */
	angular.module('icosvrWebApp.controllers.TermsCtrl', []).controller('TermsCtrl', function($scope) {

		$scope.nothing = null;

	});

});
