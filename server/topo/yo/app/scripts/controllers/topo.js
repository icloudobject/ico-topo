define([ 'angular','components/wfd3tree/wfd3tree' ], function(angular) {
	'use strict';

	/**
	 * @ngdoc function
	 * @name icosvrWebApp.controller:TopoCtrl
	 * @description # TopoCtrl Controller of the icosvrWebApp
	 */
	angular.module('icosvrWebApp.controllers.TopoCtrl', ['wfd3tree']).controller('TopoCtrl', function($scope) {

		//$log.info('from topo controller');

	});

});
